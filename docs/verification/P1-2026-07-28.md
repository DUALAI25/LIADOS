# Verificación P1 — 2026-07-28

Esta verificación cubre los 4 entregables P1 del plan de entrega certificada.

## B4 — Manual de usuario final ✅

- **Archivo**: `docs/MANUAL_CLIENTE.md` (6150 bytes, 171 líneas)
- **Contenido**:
  - Acceso al dashboard (URL + credenciales)
  - Qué hace el sistema automáticamente
  - Pantallas principales (KPIs, Gastos, Locales, Alertas)
  - Tareas habituales (buscar, reclasificar, ver PDF, exportar)
  - Qué hacer si algo falla (4 escenarios)
  - Lo que NO debes hacer (4 reglas)
  - Privacidad y seguridad
  - Soporte y contacto
  - Cambios y actualizaciones

## B5 — README técnico cerrado ✅

- **Archivo**: `README.md` (10464 bytes, 325 líneas)
- **Secciones**:
  - Qué hace (resumen ejecutivo)
  - Estado (tabla de fases con evidencia)
  - Arquitectura (diagrama ASCII)
  - Instalación rápida (entorno limpio)
  - Producción con systemd
  - Configuración (.env explicado)
  - OAuth Gmail + Drive (setup inicial)
  - Tests
  - Troubleshooting (5 escenarios)
  - Estructura del repo
  - Contribución

## B6 — pyproject.toml reproducible ✅

- **Archivo**: `pyproject.toml` (2205 bytes, 99 líneas)
- **Contenido**:
  - `[project]` con nombre, versión, descripción, deps completas
  - Dependencias: fastapi, uvicorn, jinja2, psycopg2-binary, sqlalchemy,
    google-api-python-client, openai, PyPDF2, pdfplumber, pytesseract,
    Pillow, python-dotenv, requests, tenacity, structlog, pytest
  - Dependencias opcionales: `[dev]` (black, ruff, mypy), `[e2e]` (playwright)
  - `[project.scripts]`: `liados-dashboard`, `liados-collect`, `liados-watchdog`
  - `[tool.pytest.ini_options]`: configurado
  - `[tool.setuptools.packages.find]`: incluye `agente*` y `dashboard*`
  - Compatible con `pip install -e .` y `pip install -e .[e2e]`

## B7 — Test E2E con Playwright ✅

- **Archivo**: `tests/test_e2e_dashboard.py` + `tests/conftest.py`
- **Dependencias instaladas**: `playwright==1.61.0`, `pytest-playwright==0.8.0`,
  Chromium headless 149.0.7827.55 + dependencias de sistema (libatk, libcups, etc.)
- **Resultado**: **10/10 tests PASSED en 2.44s**

| Test | Verifica | Resultado |
|---|---|---|
| test_01 | `/api/health` retorna 200 + status=ok | ✅ PASSED |
| test_02 | `/` (dashboard) renderiza HTML con términos relevantes | ✅ PASSED |
| test_03 | `/` sin auth retorna 401 | ✅ PASSED |
| test_04 | `/api/kpis` retorna KPIs con keys esperadas | ✅ PASSED |
| test_05 | `/api/facturas-recientes` retorna lista no vacía | ✅ PASSED |
| test_06 | `/api/gastos` retorna datos de gastos | ✅ PASSED |
| test_07 | `/api/search?q=Telef` responde 200 | ✅ PASSED |
| test_08 | `/api/health` retorna status=ok | ✅ PASSED |
| test_09 | `/login` renderiza HTML con form | ✅ PASSED |
| test_10 | Flujo completo: dashboard → KPIs → gastos → search → dashboard | ✅ PASSED |

## Conclusión

**98% → 99% entrega certificada.** P1 cerrado:

- B4 Manual tangible para el cliente final
- B5 README técnico para onboarding rápido
- B6 Instalación reproducible con `pip install -e .[e2e]`
- B7 Certificación formal con Playwright (10 tests E2E pasando)

**Liados está listo para entrega al cliente.**
